components:
  schemas:

    InformationChannel:
      type: string
      enum: [sms, email]
      description: > 
        Канал информирования пользователя:
        * sms - отправка СМС сообщения
        * email - отправка E-mail-а